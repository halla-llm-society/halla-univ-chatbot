name: ai-ci

on:
  workflow_call:

jobs:
  docker-build-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build ai docker image
        run: docker build -t ai-build-test ./chatbot/ai
