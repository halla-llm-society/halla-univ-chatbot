spring:
  data:
    redis:
      host: ${REDIS_HOST}
      ssl:
        enabled: true
  config:
    import: "aws-parameterstore:/chatbot/stg/"

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false
